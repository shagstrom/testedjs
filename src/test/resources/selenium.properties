# The build-machine must be visible to the machine-browser machines
# I.e., if the build machine is localhost then machine-browsers machine
# must also be localhost.
build-machine=localhost

# The line below is an example on what the machine-browsers property
# could look like. Update and uncomment to see you JavaScript tests run.
# You need to have Selenium Server running on the specified machines below.
#machine-browsers=localhost:*firefox,*safari;14.23.53.45:*iexplore

selenium-server-port=4444
jetty-port=8089
default-time-for-javascript-test-to-finish=1000
javascript-test-webroots=src/main/webapp;src/test/webapp
